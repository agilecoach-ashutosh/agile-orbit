(function(){'use strict';const rows=document.getElementById('teamRows'),summary=document.getElementById('summaryRows');if(!rows)return;const $=id=>document.getElementById(id),n=v=>Number.isFinite(parseFloat(v))?parseFloat(v):0,fmt=v=>Number.isInteger(v)?String(v):v.toFixed(1);function add(name){const tr=document.createElement('tr');tr.innerHTML='<td><input class="name" value="'+(name||'Team '+(rows.children.length+1))+'"></td><td><input class="members" type="number" min="1" step="1" value="7"></td><td><input class="holidays" type="number" min="0" step="0.5" value="0"></td><td><input class="leave" type="number" min="0" step="0.5" value="0"></td><td><input class="focus" type="number" min="0" max="100" step="1" value="'+$('focusDefault').value+'"></td><td class="avail">0</td><td class="cap">0</td><td><input class="velocity" type="number" min="0" step="0.1" placeholder="Optional"></td><td><button class="calc-delete" title="Remove team" type="button">×</button></td>';rows.appendChild(tr);tr.querySelectorAll('input').forEach(x=>x.addEventListener('input',calc));tr.querySelector('.calc-delete').onclick=()=>{tr.remove();calc()};calc()}function calc(){const weeks=n($('piWeeks').value),days=n($('daysWeek').value),base=weeks*days;let people=0,avail=0,cap=0;summary.innerHTML='';Array.from(rows.children).forEach(tr=>{const m=n(tr.querySelector('.members').value),h=n(tr.querySelector('.holidays').value),l=n(tr.querySelector('.leave').value),f=Math.max(0,Math.min(100,n(tr.querySelector('.focus').value)))/100,v=n(tr.querySelector('.velocity').value);const a=Math.max(0,m*(base-h-l)),c=a*f;people+=m;avail+=a;cap+=c;tr.querySelector('.avail').textContent=fmt(a);tr.querySelector('.cap').textContent=fmt(c);const s=document.createElement('tr');s.innerHTML='<td>'+escapeHtml(tr.querySelector('.name').value||'Unnamed team')+'</td><td>'+fmt(m)+'</td><td>'+fmt(a)+'</td><td><strong>'+fmt(c)+'</strong></td><td>'+(v?fmt(v):'—')+'</td><td>'+ (v?'Capacity = '+fmt(c)+' person-days; velocity '+fmt(v)+' pts/sprint.':'Capacity only; no velocity entered.')+'</td>';summary.appendChild(s)});$('kTeams').textContent=rows.children.length;$('kPeople').textContent=fmt(people);$('kAvailable').textContent=fmt(avail);$('kCapacity').textContent=fmt(cap);$('artInsight').innerHTML='<strong>Planning cue:</strong> The ART has <strong>'+fmt(cap)+' capacity person-days</strong> after holidays, leave and focus factor. Use this to shape objectives, dependencies and stretch conversations; do not translate person-days directly into committed scope.'}function escapeHtml(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}[$('piWeeks'),$('daysWeek'),$('focusDefault')].forEach(x=>x.addEventListener('input',calc));$('addTeam').onclick=()=>add();$('resetTeams').onclick=()=>{rows.innerHTML='';add('Team 1');add('Team 2');add('Team 3')};add('Team 1');add('Team 2');add('Team 3')})();
